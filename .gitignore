# Rust / Anchor
target/
.anchor/
test-ledger/
# solana/Cargo.lock IS committed: it is v3 with MSRV pins so `cargo build-sbf`
# works under platform-tools' older cargo. program-tests has its own lockfile.
solana/program-tests/Cargo.lock

# Node / Foundry
node_modules/
evm/deployments/
evm/test/out/
out/
cache/
broadcast/

# Local
.env
.DS_Store
